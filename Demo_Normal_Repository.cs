namespace DemoTestEffort
{
    public class Demo_Normal_Repository
    {
        public void WriteToDatabase(Counter counter)
        {
            var remainder = counter.Remainder;
            var tableName = counter.TableName;

            // wrtie to database
        }
    }
}